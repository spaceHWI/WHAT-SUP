# WHAT-SUP

제주도 SUP(패들보드) 스팟의 지금·오늘·내일·모레 적합도를 점수로 보여주고, 스팟별 항로와 다운윈드 코스를 추천하는 단일 페이지 대시보드.

## 구조와 배포

- **`index.html` 한 파일이 전부다.** HTML·CSS·JS가 모두 인라인. 빌드 도구·의존성·패키지 매니저 없음.
- 배포: **GitHub Pages, `main` 브랜치 루트**. `main`에 푸시하면 그대로 반영된다. 별도 배포 단계 없음.
- 확인은 브라우저로 `index.html`을 직접 열면 된다 (외부 API를 CORS로 호출하므로 `file://`에서도 동작).
- 데이터 소스: Open-Meteo 무료 API 두 곳.
  - 기상: `api.open-meteo.com/v1/forecast` (풍속·돌풍·풍향·기온·강수·weather_code)
  - 해양: `marine-api.open-meteo.com/v1/marine` (파고·주기·수온·조위)
  - 두 요청 모두 전 스팟 좌표를 콤마로 묶어 **한 번에** 보낸다. 응답 배열 순서 = `SPOTS` 배열 순서이므로 **`SPOTS` 순서와 응답 인덱스는 반드시 1:1로 유지**할 것.
  - 해양 API가 실패해도 앱은 죽지 않는다. `hasMarine=false`가 되고 바람 기준으로만 점수를 내며 배너를 띄운다.
- 옵션: `wind_speed_unit=ms`, `timezone=Asia/Seoul`, `forecast_days=4`, marine은 `cell_selection=sea`.

## 핵심 데이터

- `SPOTS` — 스팟 정의 배열. 현재 **16개**.
- `RUNS` — 다운윈드 편도 코스. 현재 4개. `fromId`는 반드시 `SPOTS`의 `id`와 일치해야 한다.
- 스팟 필드 의미:
  - `facing` — 해변이 **바다를 향하는** 방위(도).
  - `left` / `right` — 해안선 양쪽 랜드마크와 그 방위. 항로 추천 문구에 그대로 쓰인다.
  - `mx` / `my` — 지도 SVG 좌표 (아래 규칙 참조).
  - `shelter` — 방파제·포구로 둘러싸여 안쪽이 잔잔한 정도(0~1). 없으면 열린 해변. 점수 항목 4번 참조.
  - `lx` / `ly` / `anchor` — 지도 라벨 오프셋과 `text-anchor`. 서쪽 스팟은 보통 `anchor:"end"` + 음수 `lx`.
- **`lat`/`lon`은 실제 해안선 위여야 한다.** 예보 격자는 성기지만 지형 도형(아래)이 이 좌표를 창 중심으로 쓴다.
  과거 월령 좌표가 1.4km 내륙이라 도형이 통째로 육지로 나온 적이 있다. 새 스팟은 OSM 지명 노드 좌표를 쓸 것.
- **풍향(`wd`)은 바람이 "불어오는" 방향** 기준이다. 지도 화살표만 `wd+180`(불어가는 방향)으로 그린다. 이 규칙을 헷갈리면 이안풍 판정이 통째로 뒤집힌다.

## 점수 로직 (`scoreHour`) — 바꿀 때 주의할 규칙

100점에서 감점하는 방식. 순서와 임계값이 곧 스펙이다.

1. **풍속은 3m/s를 초과할 때부터 감점한다.** 3m/s 이하는 무감점.
   - `3 < w ≤ 5` → `(w-3)×10` (5m/s에서 -20)
   - `5 < w ≤ 7` → `20+(w-5)×15` (7m/s에서 -50)
   - `w > 7` → `50+(w-7)×10`
2. **이안풍(육지→바다 바람)은 크게 감점한다.** 판정: `angDiff(wd, (facing+180)%360) < 55°` **그리고** `풍속 ≥ 4m/s`.
   - `4 ≤ w < 6` → **-25**, `w ≥ 6` → **-40**
   - 동시에 `flags`에 `"이안풍 주의"`를 넣는다. 이 문자열은 카드 경고(`renderCards`)와 툴팁에서 그대로 매칭하므로 **문구를 바꾸면 양쪽을 같이 고쳐야 한다.**
   - ±55°와 4m/s 하한은 안전 관련 임계값이다. 완화하려면 이유를 남길 것.
3. 돌풍: `gs > ws+4` → -10, `gs > 12` → 추가 -10.
4. 파고: 0.3m 초과부터 감점 (`≤0.6`: `(wh-0.3)×67` / `≤1.0`: `20+(wh-0.6)×75` / 그 외 `50+(wh-1.0)×30`). 파고가 `null`이면 감점 없이 `"파고 정보 없음"` 플래그만.
   - **방파제 예외** — `shelter` 값이 있는 포구형 스팟(판포·신창포구·대평포구·예래포구·월령포구)은 `effWave()`로 파고를 `wh×(1-shelter)`만큼 낮춘 뒤 감점한다. 예보 파고는 열린 바다 기준이라 포구 안 실제 상태와 크게 다르기 때문.
   - 단 **원래 파고가 `SHELTER_MAX_WAVE`(1.5m) 이상이면 보정하지 않는다.** 그런 날은 포구 입구 통과 자체가 위험하다. 이때 카드에 "포구 밖으로 나가지 마세요" 경고가 뜬다.
   - 보정값은 `h.whEff`에 담긴다. **화면·차트·표는 전부 `whEff`를 쓰고, 다운윈드 판정(`renderRuns`)만 원래 `wh`를 쓴다** — 다운윈드는 포구 밖 먼바다를 달리니까. 이 구분을 지킬 것.
   - `shelter`는 파고만 낮춘다. **바람·이안풍 감점은 그대로 둔다** (방파제는 파도를 막지 바람을 막지 않는다).
5. 강수: `강수확률 ≥ 60%` 이면서 `강수량 ≥ 0.5mm` → -15.
6. 뇌우: `weather_code ≥ 95` → **점수를 0으로 덮어쓴다** (감점이 아니라 확정 0).

등급(`grade`): 75+ 최적 / 55+ 좋음 / 35+ 주의 / 그 미만 비추천. `bestWindow`의 기본 하한도 55점이라 **"좋음" 경계를 바꾸면 추천 시간대 계산도 같이 움직인다.**

## 항로 추천 (`routeAdvice`)

점수와 별개인 안전 문구 로직. 이안풍 각도 기준(±55°)은 `scoreHour`와 같게 유지한다.

- 이안풍 + `ws ≥ 6` → 출수 금지 (`danger:true`)
- 이안풍 + `ws ≥ 4` → 해안선 평행·100m 이내 (`danger:true`)
- `ws < 2.5` → 자유, `left ↔ right` 왕복 추천
- 그 외 → 풍향에 가까운 쪽으로 **맞바람으로 먼저 나갔다가 바람 등지고 복귀**. 이 순서를 뒤집으면 안 된다.

## 지도 (`renderMap`)

- SVG `viewBox="0 0 840 470"`. 제주도 외곽선은 상수 `JEJU_PATH` 하나.
- **스팟을 추가·이동할 때 `mx`/`my`는 반드시 `JEJU_PATH` 외곽선 위에 놓는다.** 스팟은 전부 해변·포구라 내륙이나 바다에 점이 찍히면 즉시 틀린 그림이 된다.
  - 대부분의 스팟은 `JEJU_PATH`의 베지어 **앵커 포인트 좌표와 정확히 일치**한다 (예: `panpo 72,258`, `iho 313,96`, `pyoseon 706,277`). 새 스팟이 기존 앵커와 맞지 않으면 곡선 위 지점을 쓰되, 눈으로 렌더링을 확인할 것.
  - 해안선 형태를 바꿔야 하면 `JEJU_PATH`를 고치고, 그 위에 얹힌 **모든** 스팟의 `mx`/`my`를 다시 맞춘다.
- 라벨이 겹치면 좌표가 아니라 `lx`/`ly`/`anchor`로 푼다.

## 지형 도형 (`SHAPES` / `shapeSvg`)

카드와 스팟 드롭다운에 스팟 주변 **실제 해안 모양**을 작게 그린다. 그림 아이콘이 아니라 OSM 실측 데이터다.

- 출처: OpenStreetMap `natural=coastline`. 제주 해안선 way를 끝점끼리 이어 **닫힌 고리**로 만든 뒤,
  스팟마다 **820m × 300m 창**으로 Sutherland–Hodgman 폴리곤 클리핑해 `0~200 × 0~73` 좌표로 옮긴 것.
  (선을 자르고 억지로 닫으면 육지/바다가 뒤집히거나 겹친다. 반드시 닫힌 고리를 클리핑할 것.)
- 창은 `facing` 만큼 회전시켜 **모든 스팟이 "위=바다, 아래=육지"** 로 보이게 맞춰져 있다.
  그래서 가로로 긴 띠에 잘라 넣어도 어색하지 않다. `facing`을 고치면 도형도 다시 생성해야 한다.
- 스팟이 포구 안쪽이라 해안선에서 150m 넘게 떨어지면 창 중심을 해안선 위로 옮긴다.
- 도형은 화면에 하드코딩된 상수다. **스팟을 추가하면 `SHAPES`에도 항목을 넣어야 한다** (없으면 빈 바다만 나온다).

## 스팟을 추가·삭제할 때 체크리스트

1. `SPOTS`에 항목 추가 (모든 필드 채우기 — `left`/`right` 누락 시 항로 추천이 깨진다).
2. `lat`/`lon`은 실제 해안선 위 좌표로. 포구면 `shelter`도 함께.
3. `mx`/`my`를 `JEJU_PATH` 외곽선 위에 배치, 라벨 겹침 확인.
4. `SHAPES`에 해당 `id`의 해안 도형 추가 (위 절 참조).
5. **검증 스크립트의 `SPOTCOUNT` 상수를 새 스팟 수와 맞춘다.** 스팟 수와 어긋나면 검증이 통째로 무의미해진다.
6. 브라우저로 열어 지도·카드·상세·다운윈드가 모두 정상인지 확인.

스크립트 없이 빠르게 개수만 확인할 때:

```bash
grep -c '{ id:"' index.html
```

## 렌더링·상태

- 전역 상태는 셋뿐: `DATA`(예보), `dayMode`(`"now"` | `0` | `1` | `2`), `selSpot`.
- `render()`가 hero·지도·카드·상세·다운윈드를 **전부 다시 그린다.** 부분 갱신 없음. 새 UI를 붙이면 `render()`에 등록할 것.
- 스팟 카드 목록은 **점수 내림차순**으로 정렬한다 (`renderCards`). 점수 없는 스팟은 맨 뒤. `SPOTS` 배열 순서는 API 응답 인덱스와 묶여 있으므로 **정렬은 렌더 시점에만** 하고 `SPOTS` 자체를 재정렬하지 말 것. 지도는 지리 정보라 정렬과 무관하다.
- `dayMode`가 `"now"`면 현재 시각 인덱스, 아니면 그날 **낮 06~19시** 중 최고 점수 시각을 대표값으로 쓴다 (`repState`).
- 카드에는 낮 시간대 점수를 한 줄 막대로 보여준다 (`sparkHtml`). "지금" 모드에서 지나간 시간은 흐리게 처리한다.
- 상세로 내려간 뒤 목록까지 스크롤을 되돌리지 않도록 **스팟 드롭다운**(`renderPicker`, `.detail-bar`)이 상단에 붙어 있다.
  드롭다운 목록과 카드 목록은 같은 `rankedSpots()` 순서를 쓴다.
- 상단 바는 `position:sticky`다. 그 높이를 `syncTopbar()`가 `--topbar-h`로 넘겨 `.detail-bar`의 sticky 위치와
  `scrollToDetail()`의 보정값으로 쓴다. **상단 바 구조를 바꾸면 이 연결을 확인할 것.**
- 상세 차트는 현재 시각부터 **48시간**. 차트는 라이브러리 없이 SVG 문자열로 직접 그린다 (`barChart` / `lineChart`).

## 다운윈드 (`renderRuns`)

- 조건 판정은 `runOK()` 하나로 모은다 (풍향 범위 · 풍속 범위 · 파고 1.2m 미만 · 뇌우 아님).
- 조건이 안 맞으면 그냥 "대기"로 끝내지 말고 `findRunWindow()`로 **다음에 열리는 낮 시간대**를 찾아 보여준다.
  제주에서 5~10m/s 정속풍은 드물어 대부분의 날은 대기다 — 이게 정상이라는 걸 화면에서 알 수 있어야 한다.
- 상단의 지금/오늘/내일/모레 선택을 따른다. `nowIdx()`만 보던 옛 코드로 되돌리지 말 것.

## 테마·스타일

- 테마 3단(자동/라이트/다크) 토글. 다크 토큰이 **`:root[data-theme="dark"]`와 `@media (prefers-color-scheme: dark)` 두 곳에 중복 정의**돼 있다. 색 하나를 바꾸면 **두 블록 다** 고쳐야 한다.
- 색은 전부 CSS 변수. SVG 안에서도 `fill="var(--shape-land)"` 식으로 변수를 쓴다 — 하드코딩 hex를 새로 넣지 말 것.
  예외는 로고뿐(아래).

### 색 규칙 — 이 세 갈래를 섞지 말 것

색을 적게 쓰는 게 이 사이트의 방침이다. 쓰는 색은 딱 세 갈래다.

1. **중립** — `--page/--surface-1/--surface-2/--ink-1~3/--grid/--axis/--border`. 화면의 대부분.
   **차트의 데이터 표시도 여기 속한다** (`--ink-1`). 차트마다 다른 색을 주지 않는다 — 어차피 차트당 계열이 하나뿐이다.
2. **키컬러 `--key` (코랄 `#e2553a`)** — 브랜드·선택·강조 전용. 섹션 번호, 선택된 날짜 알약, 선택된 카드 테두리,
   항로 추천 박스 왼쪽 선, 지도에서 선택한 스팟 테두리, 지형 도형의 현위치 점, 로고 셔츠.
   라이트·다크 양쪽에서 대비 3:1을 통과한 값이라 두 테마가 같은 hex를 쓴다. **바꾸려면 대비를 다시 검증할 것.**
3. **상태색 `--good/--warn/--serious/--critical`** — 오직 **판정**에만. 배지, 적합도 타임라인, 카드 미니 막대,
   지도·드롭다운의 점. 색각·대비 검증을 통과한 고정값이니 값을 바꾸지 말고, 다른 용도로 돌려쓰지도 말 것.
   항상 아이콘+글자와 함께 쓴다(색만으로 의미를 싣지 않는다).

키컬러와 `--critical`은 둘 다 붉은 계열이라 눈으로는 가깝다. **키컬러를 판정처럼 보이는 자리에 쓰지 말 것**,
그리고 상태색 배지는 항상 글자를 달고 나오게 할 것 — 이 둘로 구분을 유지한다.
계열이 둘 이상인 차트를 새로 만들 거라면 그때는 검증된 카테고리 팔레트를 따로 가져와야 한다(지금은 없다).

- 디자인용으로 추가한 토큰: `--r-lg/-md/-sm`(모서리), `--sh-1/-2`(그림자), `--wash`(상단 배경 그라데이션, 아주 옅게),
  `--key/--key-soft`, `--surface-2`(칩·보조면), `--shape-sea/--shape-land`(지형 도형), `--display`(타이틀 폰트), `--topbar-h`.
- 문구는 한국어, 존댓말, 과장 없는 담백한 톤. 안전 관련 문구는 단정적으로 쓴다.
- 로고(레트로 선셋 배지)는 헤더의 인라인 SVG(`.logo`)와 `<head>`의 파비콘 `data:` URL **두 곳에 같은 그림이 중복**돼 있다. 사이트 테마와 무관하게 같은 색으로 보여야 하므로 로고만은 CSS 변수 대신 hex를 쓴다.
  - **로고를 고치면 파비콘도 같이 고칠 것.** 손으로 인코딩하지 말고, 헤더 SVG를 수정한 뒤 아래 방식으로 파비콘을 다시 생성하면 어긋나지 않는다: 헤더의 `<svg class="logo">…</svg>` 블록을 뽑아 `class="logo"`를 `xmlns="http://www.w3.org/2000/svg"`로 바꾸고, `role`/`aria-label`을 지우고, 줄바꿈을 없애고, 따옴표를 홑따옴표로 바꾼 뒤 퍼센트 인코딩해서 `<link rel="icon" href="data:image/svg+xml,…">`에 넣는다.
